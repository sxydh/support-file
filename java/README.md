# Profile
Java command

# Specification
  * Base
  ```bash

  #Compile (in java file directory)
  javac App.java

  #Execute (e.g., classpath: "E:\RN\MyEclipse-Workspace\java-main-test\basics\src\main\java\gc\App.class", and package name: gc, you should cd to "E:\RN\MyEclipse-Workspace\java-main-test\basics\src\main\java\", and execute follow cmd.)
  java gc.App

  ```

  * Tools (jdk_path/bin/)
  ```bash
  
  #jconsole.exe: the JConsole graphical user interface is a monitoring tool that complies to the Java Management Extensions (JMX) specification. JConsole uses the extensive instrumentation of the Java Virtual Machine (Java VM) to provide information about the performance and resource consumption of applications running on the Java platform.

  #jps.exe: the jps tool lists the instrumented HotSpot Java Virtual Machines (JVMs) on the target system.

  ```