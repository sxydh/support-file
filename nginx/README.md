# Profile
Simple nginx guide
# Specification
* Base CMD
  ```bash
  #start server
  start nginx
  #reload server
  nginx -s reload
  #show status
  nginx -t
  #quit nginx
  nginx -s quit
  ```
* Server config
  * [`nginx.conf`](./nginx.conf)
  